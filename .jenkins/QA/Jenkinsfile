// .jenkins/QA/Jenkinsfile — QA rerun pipeline entrypoint for HorizonFrameworkDemo.
// Consumes packaged artifacts from Build/Development and reruns automation tests.

@Library('jenkins-unreal-pipeline-library') _

unrealQaPipeline(windowsAgentLabel: 'unreal-win64')